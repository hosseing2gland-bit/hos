# 🏗️ معماری جدید: Admin-Client

## 📋 خلاصه تغییرات

پروژه به دو بخش مجزا تبدیل می‌شود:

### 1️⃣ Admin Panel (مدیر)
- **هدف**: ساخت و مدیریت پروفایل‌ها
- **کاربران**: مدیران و سازندگان پروفایل
- **دسترسی**: کامل به همه امکانات

### 2️⃣ Client App (کلاینت/کاربر نهایی)
- **هدف**: فقط استفاده از پروفایل آماده
- **کاربران**: کاربران نهایی
- **دسترسی**: محدود و ساده

---

## 🎯 معماری کلی

```
┌─────────────────────────────────────────────────────┐
│                  LICENSE SERVER                      │
│  (مدیریت License، توزیع پروفایل، احراز هویت)        │
│                                                       │
│  - License Management                                │
│  - Profile Distribution                              │
│  - Client Authentication                             │
│  - Usage Tracking                                    │
└─────────────────┬───────────────────────────────────┘
                  │
         ┌────────┴─────────┐
         ▼                  ▼
┌──────────────────┐  ┌──────────────────┐
│   ADMIN PANEL    │  │   CLIENT APP     │
│                  │  │                  │
│ - ساخت پروفایل  │  │ - دریافت پروفایل│
│ - تنظیم امضا     │  │ - باز کردن URL  │
│ - تعیین URL      │  │ - استفاده ساده  │
│ - مدیریت Client  │  │ - محدود شده      │
└──────────────────┘  └──────────────────┘
```

---

## 📱 Admin Panel (Desktop App)

### ویژگی‌ها:
✅ **مدیریت کامل پروفایل‌ها**
- ساخت پروفایل جدید
- تنظیم Fingerprint (Canvas, WebGL, Audio, WebRTC, ...)
- تعیین URL پیش‌فرض برای باز شدن
- تنظیم Proxy
- مدیریت Cookie و localStorage
- Import/Export پروفایل‌ها

✅ **مدیریت Clients**
- ساخت License Key
- مشاهده لیست Client‌های فعال
- تخصیص پروفایل به Client
- غیرفعال/فعال کردن Client
- مشاهده آمار استفاده

✅ **تنظیمات پیشرفته**
- مدیریت کاربران Admin
- تنظیمات امنیتی
- Backup & Restore
- گزارش‌ها و لاگ‌ها

### ساختار UI:
```
Admin Panel/
├── Dashboard (آمار کلی)
├── Profiles (لیست و مدیریت پروفایل‌ها)
│   ├── Create Profile
│   ├── Edit Profile
│   │   ├── Basic Info (نام، تگ)
│   │   ├── Fingerprint Settings
│   │   ├── Default URL ⭐ جدید
│   │   ├── Proxy Settings
│   │   └── Cookies & Storage
│   └── Assign to Client ⭐ جدید
├── Clients (مدیریت کلاینت‌ها) ⭐ جدید
│   ├── Client List
│   ├── Generate License
│   ├── Assign Profile
│   └── Usage Statistics
└── Settings
```

---

## 📱 Client App (Desktop App - ساده شده)

### ویژگی‌ها:
✅ **فقط استفاده از پروفایل**
- ورود با License Key
- دریافت پروفایل از سرور
- باز کردن Browser با تنظیمات از پیش تعیین شده
- باز شدن خودکار URL مشخص شده توسط Admin

❌ **عدم دسترسی به:**
- ساخت یا ویرایش پروفایل
- تغییر Fingerprint
- تنظیمات پیشرفته
- کدهای داخلی

### ساختار UI (ساده):
```
Client App/
├── Login (با License Key)
└── My Profile
    ├── نمایش نام پروفایل
    ├── دکمه "Start Browser" ⭐
    └── اطلاعات ساده (نام، تاریخ انقضا)
```

### جریان کار Client:
```
1. باز کردن Client App
2. وارد کردن License Key
3. دریافت خودکار پروفایل از سرور
4. کلیک روی "Start Browser"
5. Browser با تنظیمات دقیق و URL مشخص باز می‌شود
6. کاربر فقط استفاده می‌کند، هیچ تنظیماتی ندارد
```

---

## 🔐 License Server (Node.js + Express)

### API Endpoints:

#### برای Admin:
```javascript
POST   /api/admin/licenses/generate       // ساخت License جدید
GET    /api/admin/licenses                 // لیست همه License‌ها
DELETE /api/admin/licenses/:id             // حذف/غیرفعال License
POST   /api/admin/profiles/:id/assign      // تخصیص پروفایل به License
GET    /api/admin/clients                  // لیست Client‌های آنلاین
```

#### برای Client:
```javascript
POST   /api/client/auth                    // احراز هویت با License Key
GET    /api/client/profile                 // دریافت پروفایل تخصیص داده شده
POST   /api/client/heartbeat               // ارسال وضعیت (آنلاین بودن)
```

### Database Schema:

```javascript
// License Model
{
  _id: ObjectId,
  licenseKey: String (unique),
  clientName: String,
  assignedProfile: ObjectId (ref: Profile),
  expiresAt: Date,
  isActive: Boolean,
  maxDevices: Number,
  activeDevices: [{
    deviceId: String,
    lastSeen: Date,
    ipAddress: String
  }],
  createdBy: ObjectId (ref: User),
  createdAt: Date
}

// Profile Model (اضافه شده)
{
  // ... همه فیلدهای قبلی
  defaultUrl: String,        // ⭐ جدید - URL پیش‌فرض
  forClientUse: Boolean,     // ⭐ جدید - آیا برای Client است؟
  assignedLicenses: [String] // ⭐ جدید - لیست License‌های تخصیص داده شده
}
```

---

## 🔒 امنیت و محدودیت‌ها

### Client App Security:
1. **Code Obfuscation**: کد Client را Obfuscate می‌کنیم
2. **No Source Access**: Client هیچ دسترسی به کدهای اصلی ندارد
3. **License Validation**: هر بار شروع، License چک می‌شود
4. **Device Binding**: License به Device خاص متصل می‌شود
5. **Encrypted Communication**: تمام ارتباطات رمزنگاری شده

### Admin Panel Security:
1. **Strong Authentication**: 2FA برای Admin
2. **Role-Based Access**: دسترسی‌های سطحی
3. **Audit Logging**: ثبت تمام اقدامات
4. **Encrypted Storage**: رمزنگاری پروفایل‌ها

---

## 📦 ساختار فایل‌ها

```
hos/
├── backend/                    # Backend فعلی (تبدیل به License Server)
│   └── src/
│       ├── models/
│       │   ├── License.js      # ⭐ جدید
│       │   └── ClientSession.js # ⭐ جدید
│       └── controllers/
│           ├── licenseController.js  # ⭐ جدید
│           └── clientController.js   # ⭐ جدید
│
├── admin-panel/                # ⭐ جدید - Desktop App برای Admin
│   ├── main/
│   ├── renderer/
│   │   └── src/
│   │       ├── pages/
│   │       │   ├── Clients.jsx      # ⭐ جدید
│   │       │   ├── LicenseManager.jsx # ⭐ جدید
│   │       │   └── ProfileAssign.jsx  # ⭐ جدید
│   └── package.json
│
├── client-app/                 # ⭐ جدید - Desktop App ساده برای Client
│   ├── main/
│   ├── renderer/
│   │   └── src/
│   │       ├── pages/
│   │       │   ├── Login.jsx        # ساده
│   │       │   └── BrowserLauncher.jsx # فقط یک دکمه!
│   └── package.json
│
└── shared/                     # کدهای مشترک
    ├── browser-core/           # Fingerprint engine
    └── utils/
```

---

## 🚀 مراحل پیاده‌سازی

### Phase 1: License Server (فعلاً Local)
- [ ] ایجاد License Model
- [ ] API برای ساخت و مدیریت License
- [ ] API برای Client Authentication
- [ ] API برای دریافت پروفایل

### Phase 2: Admin Panel
- [ ] کپی desktop-app فعلی به admin-panel
- [ ] اضافه کردن صفحه Clients
- [ ] اضافه کردن صفحه License Manager
- [ ] اضافه کردن فیلد "Default URL" به Profile Editor
- [ ] قابلیت Assign Profile to License

### Phase 3: Client App
- [ ] ساخت Client App ساده
- [ ] صفحه Login با License Key
- [ ] دریافت و ذخیره پروفایل
- [ ] دکمه Start Browser
- [ ] باز شدن خودکار URL

### Phase 4: Testing & Security
- [ ] تست کامل جریان Admin → License → Client
- [ ] Obfuscation کد Client
- [ ] Device Binding
- [ ] Encryption

### Phase 5: Remote Server (بعداً)
- [ ] Deploy License Server روی VPS
- [ ] HTTPS و SSL
- [ ] Domain و DNS
- [ ] Monitoring

---

## 💡 مثال کاربردی

### Scenario: فروش اکانت Instagram

#### Admin می‌کند:
1. یک پروفایل می‌سازد با نام "Instagram-Account-001"
2. Fingerprint را تنظیم می‌کند (Canvas, WebGL, ...)
3. Proxy اختصاصی تنظیم می‌کند
4. URL پیش‌فرض: `https://instagram.com`
5. یک License Key می‌سازد: `ABCD-1234-EFGH-5678`
6. پروفایل را به این License اختصاص می‌دهد
7. License را به خریدار می‌فروشد

#### Client (خریدار) می‌کند:
1. Client App را دانلود و نصب می‌کند
2. License Key را وارد می‌کند: `ABCD-1234-EFGH-5678`
3. روی دکمه "Start Browser" کلیک می‌کند
4. Browser با تنظیمات دقیق و Instagram باز می‌شود
5. ✅ هیچ دسترسی به تنظیمات ندارد!

---

## 📊 مزایای این معماری

### برای Admin:
✅ کنترل کامل روی پروفایل‌ها
✅ مدیریت متمرکز Client‌ها
✅ امکان فروش License
✅ آمار و گزارش استفاده

### برای Client:
✅ استفاده بسیار ساده
✅ نیاز به دانش فنی ندارد
✅ نمی‌فهمد پروژه چیست
✅ فقط یک دکمه!

### برای پروژه:
✅ امنیت بالاتر
✅ کنترل توزیع
✅ قابلیت کسب درآمد
✅ مقیاس‌پذیری

---

## 🎯 مرحله بعدی

الان شروع می‌کنیم از:
1. ✅ ساخت License Model و API
2. ✅ اضافه کردن فیلد defaultUrl به Profile
3. ✅ ساخت Client App ساده

**آماده‌ای شروع کنیم؟** 🚀
