# 📦 راهنمای نصب برای macOS

## نصب از DMG

### روش 1: نصب استاندارد (پیشنهادی)

1. فایل `Anti-Detect Browser-1.0.0.dmg` (یا `Anti-Detect Browser-1.0.0-arm64.dmg` برای Mac با Apple Silicon) را باز کنید
2. پنجره DMG باز می‌شود
3. فایل `Anti-Detect Browser.app` را به پوشه `Applications` بکشید (Drag & Drop)
4. نصب کامل است! ✅

### روش 2: اجرای مستقیم

1. فایل DMG را باز کنید
2. روی `Anti-Detect Browser.app` دوبار کلیک کنید
3. اگر پیام "این برنامه از یک توسعه‌دهنده ناشناس است" دیدید:
   - به **System Preferences** > **Security & Privacy** بروید
   - روی **"Open Anyway"** کلیک کنید
   - یا: روی فایل راست کلیک کنید > **Open** > **Open**

## ⚠️ حل مشکل "App cannot be opened" یا "damaged and can't be opened"

اگر macOS اجازه باز کردن برنامه را نمی‌دهد (مخصوصاً روی Mac دیگر):

### روش 1: استفاده از اسکریپت خودکار (پیشنهادی)
اگر فایل `حل-خطا.command` را دارید:
1. با دوبار کلیک روی آن باز کنید
2. اسکریپت به صورت خودکار مشکل را حل می‌کند
3. سپس DMG را باز کنید و برنامه را نصب کنید

### روش 2: از Terminal
```bash
# برای فایل DMG:
xattr -cr "/path/to/Anti-Detect Browser-1.0.0.dmg"

# برای برنامه نصب شده:
xattr -cr "/Applications/Anti-Detect Browser.app"
```

### روش 3: از System Preferences
1. System Preferences > Security & Privacy
2. در بخش "General"، روی "Open Anyway" کلیک کنید

### روش 4: از Finder
1. روی فایل راست کلیک کنید
2. **Open** را انتخاب کنید
3. در پنجره popup، **Open** را بزنید

## 🚀 اولین اجرا

بعد از نصب:
1. از Launchpad یا Applications، برنامه را باز کنید
2. اولین بار ممکن است کمی طول بکشد
3. برنامه آماده استفاده است!

## 📝 نکات

- برای Mac با Apple Silicon (M1/M2/M3): از فایل `-arm64.dmg` استفاده کنید
- برای Mac با Intel: از فایل بدون `-arm64` استفاده کنید
- اگر هر دو فایل را دارید، می‌توانید هر دو را نصب کنید

## 🔄 به‌روزرسانی

برای به‌روزرسانی:
1. نسخه جدید را دانلود کنید
2. برنامه قدیمی را از Applications حذف کنید
3. نسخه جدید را نصب کنید

## ❓ مشکل دارید؟

اگر مشکلی دارید:
1. مطمئن شوید macOS شما 10.12 یا بالاتر است
2. فایل DMG را دوباره دانلود کنید
3. از اسکریپت `حل-خطا.command` استفاده کنید یا دستور `xattr -cr` را اجرا کنید
4. برای راهنمای کامل، فایل `FIX-DAMAGED-ERROR.md` را مطالعه کنید

