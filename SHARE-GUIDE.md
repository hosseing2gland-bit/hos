# 📤 راهنمای Share و Sync پروفایل‌ها

## 🎯 استفاده مشترک از یک پروفایل

برای استفاده مشترک از یک پروفایل با دوستتان و داشتن session مشترک:

### روش 1: Export/Import پروفایل (ساده‌ترین)

#### مرحله 1: ساخت پروفایل مشترک
1. یکی از شما پروفایل جدید بسازید
2. تنظیمات را انجام دهید (Fingerprint, Proxy, etc.)
3. روی دکمه **"Share"** کلیک کنید
4. فایل JSON را ذخیره کنید

#### مرحله 2: ارسال به دوست
- فایل JSON را برای دوستتان بفرستید (از طریق Telegram, Email, etc.)

#### مرحله 3: Import در سیستم دوست
1. دوست شما نرم‌افزار را نصب می‌کند
2. روی دکمه **"Import"** کلیک می‌کند
3. فایل JSON را انتخاب می‌کند
4. پروفایل import می‌شود

### روش 2: Sync Cookies برای Session مشترک

برای اینکه login ها و cookies مشترک باشند:

#### مرحله 1: Export Cookies
1. پروفایل را Launch کنید
2. Login کنید یا cookies را set کنید
3. پروفایل را ببندید
4. در Profile Editor، دکمه "Export Cookies" را بزنید
5. فایل cookies را ذخیره کنید

#### مرحله 2: Share Cookies
- فایل cookies را برای دوستتان بفرستید

#### مرحله 3: Import Cookies
1. دوست شما پروفایل را import می‌کند
2. در Profile Editor، "Import Cookies" را می‌زند
3. فایل cookies را انتخاب می‌کند
4. حالا session مشترک دارند!

## 📦 Build کردن نرم‌افزار برای ارسال

### Build برای macOS
```bash
cd desktop-app
npm run build:react
npm run build
```

فایل نهایی در `desktop-app/dist/` خواهد بود.

### Build برای Windows
```bash
cd desktop-app
npm run build:react
npm run build -- --win
```

### Build برای Linux
```bash
cd desktop-app
npm run build:react
npm run build -- --linux
```

## 🔄 Sync Real-time (پیشرفته)

اگر می‌خواهید cookies به صورت real-time sync شوند:

### استفاده از Backend API
1. Backend را اجرا کنید
2. هر دو نفر register/login کنند
3. پروفایل را در Backend share کنند
4. از Cloud Sync استفاده کنند

### استفاده از فایل مشترک
1. یک فایل JSON مشترک در Dropbox/Google Drive قرار دهید
2. هر دو نفر از همان فایل استفاده کنند
3. بعد از هر تغییر، فایل را update کنید

## 📝 نکات مهم

1. **همیشه از یک Proxy استفاده کنید** - اگر از یک اینترنت استفاده می‌کنید، proxy یکسان تنظیم کنید
2. **Fingerprint یکسان** - برای session مشترک، fingerprint باید یکسان باشد
3. **Cookies را مرتب sync کنید** - بعد از هر login جدید، cookies را export و share کنید

## ⚠️ امنیت

- فایل‌های export شده شامل cookies و اطلاعات حساس هستند
- فقط با افراد مورد اعتماد share کنید
- از encryption استفاده کنید (در نسخه‌های بعدی اضافه می‌شود)

