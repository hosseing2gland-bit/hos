# 🚀 راهنمای سریع Share و استفاده مشترک

## 📋 سناریو: استفاده مشترک با دوست

### مرحله 1: ساخت پروفایل مشترک

**شما (شخص اول):**
1. پروفایل جدید بسازید
2. تنظیمات را انجام دهید:
   - Fingerprint (User Agent, Screen, etc.)
   - Proxy (اگر از proxy استفاده می‌کنید)
   - Chromium path
3. روی **"Share"** کلیک کنید
4. فایل JSON را ذخیره کنید (مثلاً `shared-profile.json`)

### مرحله 2: ارسال به دوست

- فایل `shared-profile.json` را برای دوستتان بفرستید
- می‌توانید از Telegram, Email, یا هر روش دیگری استفاده کنید

### مرحله 3: Import در سیستم دوست

**دوست شما:**
1. نرم‌افزار را نصب می‌کند (یا از شما می‌گیرد)
2. روی دکمه **"Import"** کلیک می‌کند
3. فایل `shared-profile.json` را انتخاب می‌کند
4. پروفایل import می‌شود

### مرحله 4: Sync Cookies برای Session مشترک

**بعد از Login:**

**شما:**
1. پروفایل را Launch کنید
2. Login کنید (مثلاً در سایت مورد نظر)
3. پروفایل را ببندید
4. پروفایل را Edit کنید
5. در بخش "Session Sharing"، روی **"Export Cookies"** کلیک کنید
6. فایل `cookies-xxx.json` را برای دوست بفرستید

**دوست شما:**
1. پروفایل را Edit می‌کند
2. در بخش "Session Sharing"، روی **"Import Cookies"** کلیک می‌کند
3. فایل cookies را انتخاب می‌کند
4. حالا session مشترک دارد! 🎉

## 🔄 Sync مداوم Cookies

هر بار که یکی از شما login جدید می‌کند:
1. Export Cookies کنید
2. برای دوست بفرستید
3. دوست Import می‌کند

## 📦 Build و ارسال نرم‌افزار

### Build برای macOS:
```bash
cd desktop-app
npm run build:mac
```

فایل در `desktop-app/dist/` خواهد بود.

### ارسال:
- فایل `.dmg` یا `.app` را برای دوست بفرستید
- یا کل پوشه `antidetect-browser` را zip کنید و بفرستید

## ✅ چک‌لیست

- [ ] پروفایل ساخته شد
- [ ] پروفایل Export شد
- [ ] برای دوست فرستاده شد
- [ ] دوست Import کرد
- [ ] Cookies Export شد (بعد از login)
- [ ] Cookies برای دوست فرستاده شد
- [ ] دوست Cookies را Import کرد
- [ ] هر دو نفر session مشترک دارند! 🎉

## 💡 نکات

1. **همیشه از یک Proxy استفاده کنید** - اگر از یک اینترنت استفاده می‌کنید
2. **Fingerprint یکسان** - برای session مشترک مهم است
3. **مرتب Sync کنید** - بعد از هر login جدید

